import { base0 } from 'src/theme/colors';
import styled from 'styled-components';
import { base03 } from 'src/theme/colors';

export const PinnedSliderContainer = styled.div`
  .slick-prev:before,
  .slick-next:before {
    color: ${base03};
  }
`;
